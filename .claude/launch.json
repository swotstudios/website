{
  "version": "0.0.1",
  "configurations": [
    {
      "name": "swot-site",
      "runtimeExecutable": "/usr/bin/ruby",
      "runtimeArgs": ["-e", "require 'webrick'; s = WEBrick::HTTPServer.new(:Port=>3000,:DocumentRoot=>'/tmp/swot-preview'); trap('INT'){s.shutdown}; s.start"],
      "port": 3000
    }
  ]
}
